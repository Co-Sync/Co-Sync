import React from 'react';
import NavBar from './NavBar.jsx';

/*
  This is the main component for the settings page. It renders the NavBar component.
  and any other components that will be added to the settings page.
*/

const Settings = () => {
  return (
    <div className="homeMain container">
      <NavBar />
    </div>
  );
}

export default Settings;