// app.js is like the app.js for frontend, but for backend

// const app = express()
// e.g routes for backend would go here, like app.get('/api/users', (req, res) => res.json(users))
// or app.use('/api/users', userRouter) where userRouter is defined in a separate file

// express server is defined here and exported to server.js
// export default app

// then you'd call app.listen in server.js